# chromadb requires onnxruntime which has no Python 3.13 wheel yet.
# Install manually in a Python 3.10 env: pip install chromadb
# chromadb>=0.5.0
faster-whisper
pypdf
sounddevice
soundfile
numpy
ollama
pyyaml

# Avatar layer
websockets==12.0
Pillow>=10.0.0   # optional — enables PNG sprite rendering in avatar_window
